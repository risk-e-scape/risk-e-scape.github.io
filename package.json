{
  "name": "risk-e-scape-certificate-site",
  "private": true,
  "license": "MIT",
  "description": "Certificate verification site for the DU Risk-E-Scape Certificate Course",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "node build.js",
    "test": "node test.js --build",
    "start": "node serve.js",
    "drafts": "node prepare-certificates.js && node generate-certificates.js --draft",
    "certificates": "node generate-certificates.js"
  },
  "dependencies": {
    "@fontsource/noto-serif-bengali": "^5.2.8",
    "pdfkit": "^0.16.0",
    "qrcode": "^1.5.4"
  }
}
